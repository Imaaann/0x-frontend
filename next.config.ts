import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["dummyimage.com", "picsum.photos", "fastly.picsum.photos", "cdn.discordapp.com"],
  },
};

export default nextConfig;
